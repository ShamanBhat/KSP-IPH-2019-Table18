import React, { Component, useState } from 'react';
import { StyleSheet, Text, View, Alert, Button, Image } from 'react-native';

export default function SaveImgHandler() {
    return(
        <View>
            <Text>Save Image Page</Text>
        </View>
    );
}